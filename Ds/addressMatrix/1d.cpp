#include<bits/stdc++.h>
typedef long long int ll;
using namespace std;

int main()
{
      int arr[5];
       cout<<(arr+4)<<"\n";  // &arr[4] => address of the last element. base address + (4*sizeof(int));
return 0;
}